<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.myapplication">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity"></activity>
        <activity android:name=".UserActivity"></activity>
        <activity android:name=".UserFlightsActivity"></activity>
        <activity android:name=".InformationActivity"></activity>
        <activity android:name=".MealsActivity"></activity>
        <activity android:name=".BuyFlightsActivity"></activity>
        <activity android:name=".FlightInfoAndWeatherActivity"></activity>
        <activity android:name=".WeatherAcvtivity"></activity>
    </application>

</manifest>